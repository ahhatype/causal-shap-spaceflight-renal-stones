# Step 2: DAG construction and expert-guided augmentation.
# See docs/methods and config/dag_spec.yaml.
#
# TODO: ingest config/dag_spec.yaml, build the dagitty object, run the
# semantic-mapping reconciliation against Robert's source DAG, write the
# reconciled graph to data/interim/.

source("../r/R/dag_utils.R")

stop("Step 2 driver not implemented yet")
