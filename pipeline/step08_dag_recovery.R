# Step 8: structural recovery comparison, R half (PC reused from Step 6, GES).
# See docs/methods §11 and r/R/discovery_pcalg.R.
#
# TODO: reuse step06d_pc_ida.R's CPDAG, run GES, score both against the
# known generating structure (structural Hamming distance, edge precision/recall).

source("../r/R/discovery_pcalg.R")
source("../r/R/io_contract.R")

stop("Step 8 (R half) driver not implemented yet")
