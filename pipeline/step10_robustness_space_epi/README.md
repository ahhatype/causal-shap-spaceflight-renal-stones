# Step 10: robustness to spaceflight-epidemiological constraints (extension)

Re-runs the narrowed pipeline under sampling regimes that emulate real
astronaut-cohort constraints: reduced sample size, widened mission-era
ranges, selection on cardiovascular fitness, selection on a narrow age
window, and isolation/environment-specific effects where estimable.
Astronaut selection (per the NASA narrative) is applied here as a sampling
constraint, not as a DAG node - see config/dag_spec.yaml's
selection_mechanisms section.

Not yet built. See docs/methods and docs/STATUS.md.
